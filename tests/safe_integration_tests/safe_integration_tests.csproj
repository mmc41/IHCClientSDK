<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- The only suite that talks to a real controller, so the only one that needs its credentials. -->
    <CopyIhcSettingsToOutput>true</CopyIhcSettingsToOutput>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="CompareNETObjects" />
    <PackageReference Include="System.Configuration.ConfigurationManager" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\ihcclient\ihcclient.csproj" />
  </ItemGroup>
</Project>