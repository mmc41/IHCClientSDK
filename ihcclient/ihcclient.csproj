<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- TargetFramework / TreatWarningsAsErrors come from Directory.Build.props. -->
    <!-- Owns the generated SOAP layer: its private SoapImpl adapters implement the generated contracts,
         so the repo-wide ban in BannedSymbols.txt does not apply here. -->
    <UsesGeneratedSoapLayer>true</UsesGeneratedSoapLayer>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>CS1591,CS1587</NoWarn>
    <Version>0.8.1</Version>
    <AssemblyVersion>$(Version)</AssemblyVersion>
    <InformationalVersion>This is a beta package</InformationalVersion>
    <Copyright>Apache License 2.0</Copyright>
    <Authors>mmc41</Authors>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="safe_integration_tests" />
    <InternalsVisibleTo Include="safe_unit_tests" />
    <InternalsVisibleTo Include="safe_project_tests" />
    <InternalsVisibleTo Include="ihc_lab" />
  </ItemGroup>
  <ItemGroup>
    <!-- Verbatim canonical .vis DTD blocks (the schema registry's single source of truth). Embedded so the
         byte-exact serializer reproduces IHC Visual's inline DTD without recomputing any DTD text. -->
    <None Remove="src\vis\schema\CanonicalDtdBlocks.dtd" />
    <EmbeddedResource Include="src\vis\schema\CanonicalDtdBlocks.dtd">
      <LogicalName>Ihc.Vis.CanonicalDtdBlocks.dtd</LogicalName>
    </EmbeddedResource>
  </ItemGroup>
  <ItemGroup>
    <!-- The findings-export grammar, published in the package so a consumer validating an export does not
         have to fetch it from the repository. 'Update' rather than 'Include': the file is already a None item
         through the SDK's default glob, and only its packing needs saying.

         Packed under schemas/ rather than as a contentFiles entry: it is a document to be read or handed to a
         validator, not an input every consuming build should copy to its own output.

         PackagePath names the destination FILE, not a folder. A trailing separator would make NuGet treat it
         as a folder and append the item's own relative path, landing the file at schemas/schemas/. -->
    <None Update="schemas\ihc_project_findings.xsd"
          Pack="true"
          PackagePath="schemas\ihc_project_findings.xsd" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI.Unshipped.txt" />
  </ItemGroup>
  <ItemGroup>
    <GlobalAnalyzerConfigFiles Include="analyser.config" />
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers" PrivateAssets="all" />
    <PackageReference Include="Microsoft.Extensions.Configuration" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" />
    <PackageReference Include="System.ServiceModel.Duplex" />
    <PackageReference Include="System.ServiceModel.Http" />
    <PackageReference Include="System.ServiceModel.NetTcp" />
    <PackageReference Include="System.ServiceModel.Primitives" />
    <PackageReference Include="System.ServiceModel.Security" />
    <PackageReference Include="System.Security.Cryptography.Xml" />
  </ItemGroup>
</Project>