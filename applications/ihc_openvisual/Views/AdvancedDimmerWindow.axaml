<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:a="using:Avalonia.Automation"
        mc:Ignorable="d" d:DesignWidth="420" d:DesignHeight="420"
        x:Class="ihc_openvisual.Views.AdvancedDimmerWindow"
        Title="Advanced dimmer properties"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False">
    <StackPanel Margin="20" Spacing="6">
        <StackPanel.Styles>
            <Style Selector="TextBlock.label">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,6,0,0"/>
            </Style>
        </StackPanel.Styles>

        <TextBlock Name="SoftOnLabel" Classes="label" Text="Soft on-time (ms)"/>
        <NumericUpDown Name="SoftOnBox" Minimum="200" Maximum="60000" Increment="100" FormatString="0"
                       a:AutomationProperties.LabeledBy="{Binding #SoftOnLabel}"/>

        <TextBlock Name="SoftOffLabel" Classes="label" Text="Soft off-time (ms)"/>
        <NumericUpDown Name="SoftOffBox" Minimum="200" Maximum="60000" Increment="100" FormatString="0"
                       a:AutomationProperties.LabeledBy="{Binding #SoftOffLabel}"/>

        <TextBlock Name="ManualRampLabel" Classes="label" Text="Manual ramp time (s)"/>
        <NumericUpDown Name="ManualRampBox" Minimum="2" Maximum="10" Increment="1" FormatString="0"
                       a:AutomationProperties.LabeledBy="{Binding #ManualRampLabel}"/>

        <TextBlock Name="MinimumLabel" Classes="label" Text="Minimum value (%)"/>
        <NumericUpDown Name="MinimumBox" Minimum="0" Maximum="100" Increment="1" FormatString="0"
                       a:AutomationProperties.LabeledBy="{Binding #MinimumLabel}"/>

        <TextBlock Name="MaximumLabel" Classes="label" Text="Maximum value (%)"/>
        <NumericUpDown Name="MaximumBox" Minimum="0" Maximum="100" Increment="1" FormatString="0"
                       a:AutomationProperties.LabeledBy="{Binding #MaximumLabel}"/>

        <TextBlock Name="LoadModeLabel" Classes="label" Text="Load characteristic"/>
        <ComboBox Name="LoadModeCombo" HorizontalAlignment="Stretch"
                  a:AutomationProperties.LabeledBy="{Binding #LoadModeLabel}">
            <ComboBoxItem>Inductive</ComboBoxItem>
            <ComboBoxItem>Capacitive</ComboBoxItem>
            <ComboBoxItem>Auto</ComboBoxItem>
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,14,0,0">
            <Button Name="OkButton" Content="OK" MinWidth="84" IsDefault="True" Click="OnOk"/>
            <Button Name="CancelButton" Content="Cancel" MinWidth="84" IsCancel="True" Click="OnCancel"/>
        </StackPanel>
    </StackPanel>
</Window>
